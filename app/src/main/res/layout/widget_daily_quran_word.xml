<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_open_area"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_daily_word_background"
    android:gravity="center_vertical"
    android:minHeight="110dp"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@android:style/TextAppearance.Material.Small"
        android:textColor="@color/widget_daily_word_accent"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_arabic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:textColor="@color/widget_daily_word_text"
        android:textDirection="rtl"
        android:textSize="30sp" />

    <TextView
        android:id="@+id/widget_transliteration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="1dp"
        android:textColor="@color/widget_daily_word_muted"
        android:textSize="13sp" />

    <TextView
        android:id="@+id/widget_meaning"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textColor="@color/widget_daily_word_text"
        android:textSize="16sp"
        android:textStyle="bold" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_context"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/widget_daily_word_muted"
            android:textSize="12sp" />

        <ImageButton
            android:id="@+id/widget_next"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/widget_next_button_background"
            android:contentDescription="@string/widget_next_description"
            android:padding="9dp"
            android:src="@drawable/widget_ic_next"
            android:tint="@color/widget_daily_word_text" />
    </LinearLayout>
</LinearLayout>
